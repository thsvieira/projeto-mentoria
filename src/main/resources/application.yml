spring:
  datasource:
    url: jdbc:postgresql://mentoria.cuxc65nabcqo.sa-east-1.rds.amazonaws.com:5432/projeto_mentoria_db
    username: projeto_mentoria_user
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
        default_schema: my_schema